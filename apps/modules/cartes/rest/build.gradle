group 'net.dkahn.starter.apps.modules.cartes.rest'

apply plugin: 'org.springframework.boot'


dependencies {
    compile project(':apps:modules:cartes:service')
    compile project(':tools:common')
}

bootRepackage.enabled = false
