group 'net.dkahn.starter.apps.modules.prelevements.rest'

apply plugin: 'org.springframework.boot'


dependencies {
    compile project(':apps:modules:prelevements:service')
    compile project(':tools:common')
}

bootRepackage.enabled = false
