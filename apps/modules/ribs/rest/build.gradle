group 'net.dkahn.starter.apps.modules.ribs.rest'

apply plugin: 'org.springframework.boot'


dependencies {
    compile project(':apps:modules:ribs:service')
    compile project(':tools:common')
}

bootRepackage.enabled = false
